
# Basic Laravel Application Assignment

Everything should be here. To get up and running, it should be a
matter of unzipping the project, setting up your .env file to point
at the appropriate MySQL database, and then just going through with:


```
composer install
php artisan migrate:refresh
php artisan db:seed
php artisan serve
```

